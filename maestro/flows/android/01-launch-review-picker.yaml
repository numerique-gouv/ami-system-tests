appId: ${APP_ID}
tags:
  - staging-only
  - android-only
# Ce flow s'applique uniquement au flavor staging (fr.gouv.ami.staging).
# Le flavor local (fr.gouv.ami.local) démarre directement sur FranceConnexionScreen ou Home.
---
- runFlow: ../../subflows/_launch.yaml
- runFlow: ../../elements/_loadAll.yaml

- assertVisible: "${output.reviewPicker_title}"
